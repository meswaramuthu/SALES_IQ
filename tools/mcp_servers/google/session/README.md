# stratova-session-mcp
Firestore-backed visitor session state — page views, intent, session data

## Environment Variables
- `FIRESTORE_PROJECT_ID`

## Deploy
```bash
bash deploy.sh
```

## Local development
```bash
pip install -r requirements.txt
python server.py
```
